# OS / editor junk
.DS_Store
Thumbs.db
*.log
.idea/
.vscode/
*.swp

# Local build scratch (fonts & map sources live elsewhere; everything ships embedded in index.html)
/fonts-src/
/map-src/
chars.txt

# Keep it clean — no dependencies required for this static site
node_modules/
